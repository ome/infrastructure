---
# Playbook for provisioning IDR Docker nodes

- hosts: idr-docker
  roles:
  - role: docker-storage
  - role: lvm-partition
    lvname: scratch
    lvmount: /scratch
    lvsize: "{{ scratchsize }}"
