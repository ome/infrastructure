---
# Playbook for provisioning Jenkins CI nodes

- hosts: ci-jenkins-linux
  roles:
  - role: lvm-partition
    lvname: jenkins-workdir
    lvmount: "{{ jenkinsworkdir }}"
    lvsize: "{{ jenkinssize }}"
  - role: jenkinsslave
