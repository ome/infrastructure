---
# Playbook for provisioning Devspace

- hosts: devspace

  vars_prompt:
    - name: "jenkins_nginx_password"
      prompt: "Enter Jenkins password"
      private: yes
      when: jenkins_nginx_username|length>0

  roles:
    - role: upgrade-distpackages
    - role: docker
    - role: versioncontrol-utils
    - role: devspace
