---
# Playbook for provisioning Jenkins CI nodes

- hosts: ci-jenkins-linux
  roles:
    - role: jenkinsslave
    - role: omero-build
    - role: omero-runtime
    - role: postgres
    - role: omero-build-cpp
  vars:
    - javajdkversions:
      - 1.8.0
