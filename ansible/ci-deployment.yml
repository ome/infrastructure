---
# Playbook for provisioning Jenkins CI nodes

- hosts: ci-jenkins-linux
  roles:
    - role: jenkinsslave
    - role: omero-build
    - role: omero-runtime
    - role: omero-build-cpp
    - role: postgresql
    - role: nginx
    - role: sphinx-build
  vars:
    - javajdkversions:
      - 1.8.0
