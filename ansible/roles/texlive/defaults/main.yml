---

texlive_dest: /opt/texlive
texlive_version: 2015
# The above is duplicated (!) in the installer profile file - ../files/
# Be good to get the following in here, and have them written to the 'installer profile' file.
#TEXDIR /opt/texlive/2014
TEXDIR: "{{ texlive_dest }}/{{ texlive_version }}"
TEXMFLOCAL: "{{ texlive_dest }}/texmf-local"
TEXMFSYSCONFIG: "{{ TEXDIR }}/texmf-config"
TEXMFSYSVAR: "{{ TEXDIR }}/texmf-var"
TEXMFVAR: "~/.texlive{{ texlive_version }}/texmf-var"
