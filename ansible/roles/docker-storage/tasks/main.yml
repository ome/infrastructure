---
# Setup LVM partitions for Docker

- name: storage | setup lvm docker-pool
  lvol:
    vg: "{{ vgname }}"
    lv: docker-pool
    size: "{{ dockerpoolsize }}"
    opts: "{{ lvm_lvopts | default(None) }} --thin --poolmetadatasize {{ dockermetadatasize }}"
