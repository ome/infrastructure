---
# tasks file for roles/sphinx-build

- name: sphinx documentation packages | install docs tools
  yum:
    name: "{{ item }}"
    state: latest
  with_items:
    - ant
    - ant-contrib
    - doxygen
    - git
    - graphviz
    - make

- name: sphinx documentation packages | pip install packages
  pip:
    name: "{{ item.name }}"
    state: present
    version: "{{ item.version }}"
  with_items:
    - name: sphinx
      version: 1.2.3
